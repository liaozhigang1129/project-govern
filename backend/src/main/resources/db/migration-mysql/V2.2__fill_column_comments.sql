-- ============================================================
-- V2.2 补全所有业务表字段描述 (MySQL 版, NoOp 兜底)
-- ============================================================
-- 背景: 早期此文件直接复用了 PG 版本, 含有 PG 专属语法 (COMMENT ON FUNCTION/COLUMN),
--       MySQL 8.0 跑不动. Flyway 一直在 success=0 卡住, 阻塞后续 V2.3+ 迁移.
-- 现状: 此文件已由 V2.2.1 接管 (用 ALTER TABLE ... MODIFY COLUMN 补注释).
--       MySQL 跑此文件当作 NoOp (SELECT 1), 保持 Flyway 链路连续.
-- 注: 不要删除此文件 (Flyway 按 checksum 校验), 内容已简化.
-- ============================================================

SELECT 1;
