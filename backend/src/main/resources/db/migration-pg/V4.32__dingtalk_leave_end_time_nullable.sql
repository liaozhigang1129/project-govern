-- ============================================================
-- V4.32 钉钉请休假 end_time 改为可空
-- 原因: 部分审批(加班/补卡)无明确结束时间,旧 NOT NULL 会导致整批 477 条
--       leave 全部 rollback, 同步一直 FAILED. 改为可空后业务展示用 - 表示。
-- ============================================================
ALTER TABLE dingtalk_leave ALTER COLUMN end_time DROP NOT NULL;
